---
'@web-widget/helpers': patch
---

Fix metadata merging to preserve repeatable Open Graph values, replace singleton canonical, charset, and HTTP-equivalent declarations, and avoid retaining references to input descriptors.
