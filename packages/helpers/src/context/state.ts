export {
  /**
   * @deprecated use `import { useWidgetAsyncState } from '@web-widget/helpers/state'` instead of `import { useWidgetAsyncState } from '@web-widget/helpers/context'
   */
  useWidgetAsyncState,
  /**
   * @deprecated use `import { useWidgetSyncState } from '@web-widget/helpers/state'` instead of `import { useWidgetSyncState } from '@web-widget/helpers/context'
   */
  useWidgetSyncState,
  /**
   * @deprecated use `import { useAllWidgetState } from '@web-widget/helpers/state'` instead of `import { useAllWidgetState } from '@web-widget/helpers/context'
   */
  useAllWidgetState,
} from '../state/widget-state';
