export * from './index';

/**
 * @deprecated use `import { IS_CLIENT } from '@web-widget/helpers/env'` instead of `import { IS_CLIENT } from '@web-widget/helpers'
 */
export const IS_CLIENT = true;

/**
 * @deprecated use `import { IS_SERVER } from '@web-widget/helpers/env'` instead of `import { IS_SERVER } from '@web-widget/helpers'
 */
export const IS_SERVER = false;
