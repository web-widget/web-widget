// Runtime placeholder for the type-only @web-widget/schema package.
// All exports are TypeScript types (interface/type) with no runtime values.
// This empty module prevents UNRESOLVED_IMPORT errors when consumers use
// value imports instead of `import type`.
export {};
