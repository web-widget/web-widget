// This entry re-exports deprecated APIs for backward compatibility.
// User-facing APIs (defineRouteComponent, defineMeta, etc.) are imported
// from @web-widget/helpers. Adapter implementation is in ./adapter.

/** @deprecated Import from '@web-widget/vue2/adapter' instead. */
export { asReactWidget, toReact } from './as-react-widget';
